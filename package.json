{
    "name": "terminal-app",
    "private": true,
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "tsc --noEmit",
        "build": "vite build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "typescript": "^5.0.0",
        "vite": "^6.0.0",
        "vite-plugin-checker": "^0.8.0"
    },
    "dependencies": {
        "xterm": "^5.3.0",
        "xterm-readline": "^1.1.2"
    },
    "compilerOptions": {
        "baseUrl": ".",
        "paths": {
            "@/*": [
                "src/*"
            ]
        }
    }
}
