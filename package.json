{
    "name": "terminal-app",
    "private": true,
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "typescript": "^5.0.0",
        "vite": "^6.0.0",
        "vite-plugin-checker": "^0.8.0",
        "@types/node": "^20.0.0"
    },
    "dependencies": {
        "xterm": "^5.3.0",
        "xterm-addon-fit": "^0.8.0"
    },
    "compilerOptions": {
        "baseUrl": ".",
        "paths": {
            "@/*": [
                "src/*"
            ]
        }
    }
}